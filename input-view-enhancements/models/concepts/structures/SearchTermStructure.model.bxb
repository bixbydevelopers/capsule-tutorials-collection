structure (SearchTermStructure) {
  description (Structure for making the search term look better)
  property (searchTerm) {
    type (SearchTerm)
    min (Required) max (One)
  }
  property (icon) {
    type (viv.core.Text)
    min (Required) max (One)
    visibility (Private)
  }
  property (description) {
    type (viv.core.Text)
    min (Required) max (One)
    visibility (Private)
  }
}
